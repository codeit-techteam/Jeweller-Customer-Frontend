/**
 * Campaign / collection imagery — re-exports from `imageUrls` (single source of truth for remote assets).
 */
export {
  CATEGORY_HOME_IMAGE,
  DEFAULT_JEWELLERY_FALLBACK,
  IMAGE_FALLBACK_SECONDARY,
  PLACEHOLDER_IMAGE_URI,
  COLLECTION_HERO_URIS,
} from './imageUrls';
